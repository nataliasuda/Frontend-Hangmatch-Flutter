import 'package:flutter/material.dart';

class SearchingLocation extends StatelessWidget {
  const SearchingLocation({super.key, required String sessionId});
  @override
  Widget build(BuildContext context) => const Center(child: Text('Searching for location...'));
}
